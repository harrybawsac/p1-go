```markdown
# Contract test placeholders

Tests to add under `tests/contract/`:

- test_meter_payload_schema_validation.go — validates that parser accepts the schema in `meter_payload.schema.json` and rejects invalid payloads.

These should be generated as failing tests before implementation.
```
