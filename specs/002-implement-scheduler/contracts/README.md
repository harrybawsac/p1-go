```markdown
# Contracts for scheduler feature

This directory contains API contract files and test placeholders used by Phase 1.

The CLI is not an HTTP server; contracts here describe the expected JSON payload from the meter (input contract) used by parser tests.
```
